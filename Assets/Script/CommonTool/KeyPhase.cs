using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KeyPhase : MonoBehaviour
{
[UnityEngine.Serialization.FormerlySerializedAs("boxgroup")]    [UnityEngine.Serialization.FormerlySerializedAs("Railroad")]public List<GameObject> Cohesive;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
